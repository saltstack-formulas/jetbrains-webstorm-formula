#!/usr/bin/env bash

{%- set appname = '%s%s'|format(appname, '' if not edition else edition) %}

Source={{ appname }}.app
Destination="{{ homes }}/{{ user }}/Desktop"
/usr/bin/osascript -e "tell application \"Finder\" to make alias file to POSIX file \"$Source\" at POSIX file \"$Destination\""
